// Import the module

const combined = require('./combined.js')

// Access properties
console.log(combined.message); // Output: Welcome!
console.log(combined.status);  // Output: Active
// console.log(combined.sayHi()); // This will be undefined